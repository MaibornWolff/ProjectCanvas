name: Prettier Test
on:
  pull_request:
    branches:
      - main
jobs:
  test:
    name: Prettier Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install packages
        run: yarn install
      - name: Prettier
        run: yarn prettier
